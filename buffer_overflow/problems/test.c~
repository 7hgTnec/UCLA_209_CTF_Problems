#include<stdio.h>

struct myStruct {
  int arr[8];
};

int main()
{
  unsigned test = 123;
  printf("%d\n", sizeof(struct myStruct));
  printf("%d\n", sizeof(test));

  char s[20] = "-123,123";
  int count;

  sscanf(s, "%d,", &count);
  printf("%d", count);
  return 0;
}
